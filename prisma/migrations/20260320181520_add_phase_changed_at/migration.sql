BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[specs] ADD [phase_changed_at] DATETIME2 NOT NULL CONSTRAINT [specs_phase_changed_at_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
